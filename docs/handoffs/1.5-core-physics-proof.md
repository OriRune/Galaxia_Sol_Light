# Packet handoff

Packet:

- 1.5 - Core physics proof

Files created/changed:

- Pure core calibration harness, named fixtures, reference/gate test, raw evidence, and preliminary ADR 0004.

Specification requirements:

- SPEC.md sections 8 and 18.5–18.7; IMPLEMENTATION_PLAN.md sections 8.2–8.12.

Behavior completed:

- Implements pure pairwise Plummer acceleration, two momentum-conserving overlap-friction half impulses, kick–drift–kick ordering, start-of-step merger eligibility, and deterministic remnant construction.
- Provides named default-attraction, Scenario 5 orbit/flyby, slow-capture, and merger fixtures.
- Runs the Scenario 5 no-merger orbit, slow capture, fast first pass through t=6, eventual flyby capture, asymmetric merger, and momentum residual proof.
- Compares the starting tuple to every independent core reference at 1e-6 tolerance without freezing constants.

Tests added:

- `tests/unit/physics-calibration.test.ts`: reference equality, internal core margins, non-merger/capture timing, merger fields, and friction momentum residual.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npx vitest run --project unit tests/unit/physics-calibration.test.ts`
- `npm run build`

Results:

- Pass. All six independent numerical references differ by less than 4.5e-9.
- Pass. Scenario 5 remains unmerged through t=60; slow capture merges at t=139.9666666667; flyby remains unmerged through t=6 and is captured at t=24.5666666667.
- Pass. Maximum friction momentum residual is zero and the deterministic remnant preserves exact mass-weighted momentum.
- Starting tuple passes every Task 1.5 core gate; no grid search was needed and constants remain preliminary.

Windows/Linux status:

- Deterministic platform-independent proof passes on Windows ARM64; Linux release-stage verification remains scheduled for Milestone 9.

Golden/constants changed:

- None; starting constants are measured but explicitly not frozen.

ADR/evidence:

- `docs/adr/0004-physics-calibration.md`
- `docs/evidence/core-physics-proof.json`

Remaining packet blockers:

- None.

Next packet:

- 2.1
