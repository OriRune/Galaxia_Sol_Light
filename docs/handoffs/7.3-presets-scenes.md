Packet:

- 7.3 Presets and scenes

Behavior completed:

- Added strict portable preset/scene creation, import, export, coherent Worker scene capture, and 1 MiB pre-read rejection.
- Added complete saved-preset and saved-scene UI for save, discover, load/apply, rename, delete, import, and export.
- Imported preset identity never overwrites a local row; a new cryptographic ID and collision-safe display name are allocated.
- Scene saves use `REQUEST_SCENE_SETUP`; portable downloads use the exact strict envelopes and normalized filenames.
- Single preset application replaces/enables framing, while multi-galaxy modes add without clearing.

Tests and results:

- Portable schema/repository/load unit suites pass.
- Browser workflow exercises saved preset/scene load and all CRUD/export controls.
- Hosted production smoke exports and re-imports both a preset and a scene.

Next packet:

- 7.4 Atomic versioned load
