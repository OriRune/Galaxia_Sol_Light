Packet:

- 10.3 Production promotion

Specification requirements:

- SPEC sections 17.2 and 18; plan sections 2.1, 17.2, and 19.

Promotion prerequisites:

- Task 10.2 preview deployment is recorded and fully green.
- The preview deployment corresponds to the intended release commit.
- Hosted Chromium and Firefox tests include the Vercel-applied CSP assertions.

Owner-operated commands and records:

1. From the same linked checkout and verified commit, run `npm run deploy:production` only after every preview-hosted test passes.
2. Record the production URL, deployment ID, commit SHA, and promotion time.
3. Set `PLAYWRIGHT_BASE_URL` to the production URL and run `npm run e2e:hosted`.
4. Run the current Edge and Firefox smoke on the reference Windows device.
5. Add the deployment evidence and final result to `docs/ACCEPTANCE_REPORT.md`.

Current result:

- Production promotion: complete by repository-owner attestation on 2026-07-16.
- The owner confirmed that the manually completed Vercel deployment may be marked done. The production URL and deployment ID were not supplied to this workspace, so this record does not invent them.

Release rule:

- Milestone 10 is closed on the owner's explicit completion attestation; locally reproducible release evidence remains recorded in `docs/ACCEPTANCE_REPORT.md` and `docs/PLATFORM_RESULTS.md`.
