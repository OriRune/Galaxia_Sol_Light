Packet:

- 9.5 Soak, accessibility, and report

Files created/changed:

- `tests/unit/release-soaks.test.ts`
- `e2e/release/real-recording-soak.spec.ts`
- `e2e/acceptance/17-accessibility-reliability.spec.ts`
- `perf/playwright.soak.config.ts`
- `scripts/verify-production-hooks.mjs`
- `docs/ACCEPTANCE_REPORT.md`
- `src/app/App.tsx`, `src/app/initialGalaxy.ts`, `src/main.tsx`
- `src/app/selectionService.ts`, `src/rendering/PixiViewport.ts`

Specification requirements:

- SPEC sections 15 through 18; plan sections 10.10, 14.9, 15.6, 15.7, and 19.

Behavior completed:

- Completed every required soak, accessibility smoke, context-loss gate, and production-hook absence gate.
- Added true mode-tab semantics, executable global keyboard commands, dialog focus return/trapping, and WebGL resource reconstruction after context restoration.
- Test-only Low/High fixtures and the benchmark entry are now present only when `VITE_TEST_HOOKS` is exactly `true`.

Tests added:

- Thirty-minute generated-action and internal-recording fake-clock soaks.
- Full real 120-second exact High-fixture recording soak.
- Landmark/form/keyboard/dialog and Chromium/Firefox context-loss E2E.
- Production bundle forbidden-hook scanner.

Commands run:

- `npm run soak:recording`
- Targeted unit/browser/E2E soak, lifecycle, accessibility, and context-loss suites.
- `npm run build`
- `npm run verify:production-hooks`
- `npm audit --omit=dev --json`

Results:

- All Task 9.5 gates pass, including the 2.1-minute real recording and 3,600-slot finalization.
- Production dependency audit: zero vulnerabilities.
- Acceptance report records every normative requirement group and all seventeen scenarios as passing.

Windows/Linux status:

- Milestone 9 Windows and Linux records remain green; Task 9.5 cross-engine browser gates pass in Chromium and Firefox.

Risks or follow-ups:

- None.

Next packet:

- 10.1 Production configuration
