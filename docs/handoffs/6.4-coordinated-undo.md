# Task 6.4 — Coordinated undo

- Defined the closed undoable action-kind set and exact UI snapshot shape (camera and automatic framing excluded).
- Added pause/restore/UI-pop-release coordination, failure retention, empty-stack behavior, and no redo.
- Retained the 20-entry cap and immediate oldest snapshot release.
- Worker undo restore forces paused state, clears rewind, and starts a fresh history timeline.
- Table and transaction tests cover the closed list, ordering, failure, cap, and empty stack.

Product integration audit: the shell retains snapshots only after `ACK CHANGED`, supports UI-only performance/trails transactions, restores Worker and UI state atomically, pauses, clears rewind, releases the consumed snapshot, and preserves the stack on failure. Authoritative undo also cancels focused local numeric/name strings so restored values are immediately visible. Chromium acceptance verifies consecutive gravity/speed undo to exact prior values.
