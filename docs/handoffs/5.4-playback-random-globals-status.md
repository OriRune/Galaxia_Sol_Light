# Task 5.4 — Playback, Random, globals, and status

Implemented pure playback, seed parsing/reroll, Random replacement, performance-level, and regeneration plans. Startup matches First Light exactly. Pause changes only playing state and retains selected speed. Performance changes copy the scene without modifying explicit counts. Regeneration carries stored seeds. Random uses the deterministic generator and commits replacement, deselection, automatic framing, and playing state together; invalid seed text returns no plan.

The top bar now exposes mode tabs, play/pause, step, speed, gravity, performance, trails, automatic framing, camera reset, screenshot, recording, help, FPS, playback state, and counts. Low-frequency status/notices remain in the shell store.

Five focused tests pass, plus TypeScript, ESLint, and Chromium/Firefox shell regression.

Product integration audit: mode transitions use the pure reducer and only request a coherent Worker setup when entering Single requires replacement. Random and draft rerolls use secure uint32 values. Gravity/speed, performance, and trails are transactionally undoable; the top status renders the complete required live set. Single startup links its draft to the actual initialized fixture/topology rather than a hard-coded UI default.
