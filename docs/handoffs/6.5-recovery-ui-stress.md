# Task 6.5 — Recovery UI and stress

- Worker failure now preserves the rendered canvas, disables live simulation, and exposes checkpoint step/age.
- Exact checkpoint restore and compatible-frame regeneration are distinct explicit choices; regeneration warns that evolved star motion is lost and never runs silently.
- Both restart paths initialize paused and clear rewind state without creating undo entries.
- Checkpoint replacement remains outside Zustand; real-Worker recovery/protocol tests pass.
- Deterministic worst-state memory accounting passes the 192 MiB retained and 224 MiB transient gates with the ten-state history cache cap.
