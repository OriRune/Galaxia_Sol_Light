Packet:

- 10.2 Preview deployment

Specification requirements:

- SPEC sections 12 through 14 and 17.2; plan sections 2.1 and 17.2.

Prepared behavior and evidence:

- The production build, SPA fallback, Worker asset, and local hosted Chromium/Firefox workflow are verified before deployment.
- `e2e/hosted.spec.ts` covers startup, WebGL, Worker readiness, preset and scene persistence, import/export, screenshot capture, a ten-second recording and ZIP part, refresh rediscovery, and deep-route loading.
- The hosted-only security-header case checks the Vercel CSP against remote script and image fixtures.

Owner-operated commands:

1. Authenticate, then run `npx vercel link` from the project root and select the intended owner-provided account/team and project.
2. Run `npm ci` and `npm run verify` from this exact commit.
3. Run `npm run vercel:build`.
4. Run `npm run deploy:preview` and capture its HTTPS preview URL and deployment ID.
5. In PowerShell, set `$env:PLAYWRIGHT_BASE_URL` to that URL and run `npm run e2e:hosted`. On Linux, run `PLAYWRIGHT_BASE_URL=https://the-preview-url npm run e2e:hosted`.
6. Clear `PLAYWRIGHT_BASE_URL` after the hosted run so later local tests use their local server.
7. Record the preview URL, deployment ID, commit SHA, browser versions, and command output here or in `docs/ACCEPTANCE_REPORT.md`.

Current result:

- Release candidate preparation: pass.
- Authenticated Vercel preview deployment and hosted-header verification: pending repository owner action by explicit user instruction.

Promotion rule:

- Do not begin Task 10.3 until every hosted Chromium and Firefox case, including the Vercel-header cases, passes against this preview.
