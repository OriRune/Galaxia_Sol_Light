Packet:

- 8.1 Capture library

Behavior completed:

- Artwork-only PNG capture persists only after successful encoding and never pauses simulation.
- Captures use collision-safe UTC names and remain discoverable after reload.
- Capture UI provides preview, rename, confirmed delete, and normalized PNG download with object-URL cleanup.
- Null encodes save nothing and report the encoder failure.

Tests and results:

- Capture service and repository tests cover clean persistence, names, preview, and null rollback.
- Browser workflow verifies capture discovery/detail/download controls.
- Hosted production smoke captures, previews, downloads, refreshes, and rediscovers the image.

Next packet:

- 8.2 Recording preflight and scheduler
