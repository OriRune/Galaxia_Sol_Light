# Task 7.5 — Migration, rollback, and cleanup

- Future database versions enter stable no-library mode without deletion or downgrade.
- Additive version-two migration fixture passes.
- Startup finishes deleting recordings, converts abandoned recording rows to interrupted with exact counts/ranges, and removes confirmed orphan frames in yielding batches of 100.
