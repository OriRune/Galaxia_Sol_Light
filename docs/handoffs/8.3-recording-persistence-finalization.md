# Task 8.3 — Frame write, finalization, and interruption

- Added attempted metadata, frame-first writes, count commits, missed coalescing, recount finalization, and all terminal reasons.
- Startup recovery repairs interrupted rows and exact missing ranges.

Product integration audit: the shell writes each encoded frame before advancing committed counts, coalesces missed ranges, finalizes from a recount, and refreshes the recording library for every terminal reason. Repository creation is atomic and collision-safe; interrupted rows are repaired during startup without touching completed library data. Chromium acceptance duration-finalizes a real ten-second recording at exactly 300 nominal/effective slots and verifies persisted frames.
