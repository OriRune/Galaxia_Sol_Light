# Task 5.3 — Draft, add, selected edit, and delete

Implemented validated pure plans for Single linked-draft commits, multi-mode add, apply-to-selected, delete, performance-level draft reset, and budget warning. Added the one-at-a-time command dispatcher: plan validation precedes snapshot acquisition, ACK precedes synchronous UI commit, and one undo placeholder is pushed only after success.

Rules covered include immediate Single regeneration for generation changes, name-only non-regeneration, independent multi-mode draft, add at exact viewport center with zero velocity/sole selection, selected generation apply retaining name/placement/velocity, globally valid above-level counts without clamping, global limit rejection before Worker traffic, performance changes preserving current draft, new/reset defaults by level, no Single delete, and usable empty scenes.

The shell now renders the documented draft controls, regeneration warning, conditional add/apply/delete actions, and distinct center/selection/non-zero-velocity legend. Product text uses Size and contains no forbidden label.

Seven focused workflow/transaction tests pass, along with TypeScript, ESLint, and both-browser shell regression.

Product integration audit: exact numeric edit strings are local, grammar/range checked, committed on Enter/blur, and restored by Escape. In Single, generation and name commits patch the linked galaxy immediately and retain its live placement/velocity; in multi modes the draft stays independent. Add uses the live viewport center, apply-to-selected and delete are wired, and authoritative scene replacement cancels stale local edits. A visible performance-budget warning is shown for valid explicit configurations above the selected automatic budget.
