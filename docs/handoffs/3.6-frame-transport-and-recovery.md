# Packet handoff

Packet:

- 3.6 - Production frame transport and recovery

Files created/changed:

- Production three-buffer pool, FRAME contract/publication, client topology join, exact Engine checkpoints, Worker recovery emission/restore, and tests.

Behavior completed:

- Maintains exactly three interleaved position buffers, one outstanding lease, three-miss rebuild, topology resize, and late-old return rejection.
- Publishes FRAME only from TICK with exact cores, bounds, revision, topology epoch, step index, and transferred positions.
- Client requires the committed descriptor/topology join, revision monotonicity, exact star count, and core ID order; it returns every buffer in `finally`.
- Creates exact checkpoints after INIT, acknowledged changes, requests, and each active second, including mutable banks, topology/styles/ownership, effects, globals, step/accumulator, and active wall time.
- Retains checkpoint receipt age outside React and replaces the Worker to restore the checkpoint paused under a new monotonic session revision.
- Old-instance callbacks are ignored by Worker-generation guard and staged transactions are discarded on replacement.

Tests added/expanded:

- `tests/unit/frame-pool.test.ts`: lease invariant, return, rebuild, late return, and topology resize.
- `tests/browser/protocol-transaction.test.ts`: synchronized real FRAME application/return and exact new-Worker checkpoint digest restoration with age disclosure.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npm test`
- Focused Chromium/Firefox protocol/Worker/client/app tests
- `npm run build`

Results:

- Pass: 16 unit files and 165 tests; 36 focused browser tests; architecture and production build pass.

Windows/Linux status:

- Windows Chromium and Firefox complete; Linux conformance remains scheduled for Milestone 9.

Golden/constants changed:

- None.

Remaining packet blockers:

- None.

Next packet:

- 3.7
