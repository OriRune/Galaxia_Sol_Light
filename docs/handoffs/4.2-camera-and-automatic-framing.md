# Task 4.2 — Camera and automatic framing

Implemented the exact world/CSS transforms, zoom clamp `0.02..100`, point-centered zoom, CSS-delta pan, reset defaults, immediate live-bounds framing, and the closed automatic-framing reducer. The production shader now receives the correct DPR-scaled X/Y world transforms, including the inverted screen Y axis.

Focused verification:

- Camera unit suite passes all six tests: round trips at minimum/default/maximum zoom, exact point zoom, empty defaults, 20% padded union framing including core radii, and every automatic-framing reducer row.
- TypeScript and ESLint pass.
- Existing Pixi viewport lifecycle/production-renderer suite remains green in Chromium and Firefox (8 tests total).
