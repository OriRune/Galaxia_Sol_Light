# Packet handoff

Packet:

- 1.3 - History memory proof

Files created/changed:

- Standalone snapshot/topology prototype, 120,000-star proof test, deterministic evidence, and ADR 0002.

Specification requirements:

- SPEC.md sections 11.1 and 17.2; IMPLEMENTATION_PLAN.md sections 8.1, 11.1–11.6, and 15.7.

Behavior completed:

- Allocates 31 topology-churn keyframes, a post-keyframe merger, 20 undo snapshots, ten cached reconstructions, dual live banks, pinned present, reconstruction candidate, three frame buffers, and main typed-array caches concurrently.
- Shares immutable five-byte-per-star StyleBlocks through explicit reference counts and rebuilds owner slots from Segment ranges.
- Verifies clone/restore with a little-endian SHA-256 state digest.
- Enforces immediate LRU eviction across nineteen regular markers and a separate 300 dense single-step-marker replay.
- Counts every owned ArrayBuffer by identity so shared style bytes are counted once.

Tests added:

- `tests/unit/history-memory.test.ts`: exact digest, reference sharing, owner reconstruction, keyframe/undo/cache cardinalities, dense and regular eviction counts, and both byte ceilings.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npx vitest run --project unit tests/unit/history-memory.test.ts`
- `npm run build`

Results:

- Pass. Clone/restore SHA-256 digests are identical.
- Pass. Retained accounting is 149,880,000 bytes (142.94 MiB), below 192 MiB.
- Pass. Transient accounting is 151,800,000 bytes (144.77 MiB), below 224 MiB.
- Pass. Nineteen-marker replay retains ten with nine evictions; dense 300-marker replay retains ten with 290 evictions.

Windows/Linux status:

- Deterministic platform-independent proof passes on Windows ARM64; Linux release-stage verification remains scheduled for Milestone 9.

Golden/constants changed:

- None.

ADR/evidence:

- `docs/adr/0002-history-storage.md`
- `docs/evidence/history-memory-proof.json`

Remaining packet blockers:

- None.

Next packet:

- 1.4
