# Task 6.2 — Markers, keyframes, and log

Implemented the Worker-owned `HistoryTimeline` with one event-ordinal source for commands, merger expectations, and markers.

- Exact regular markers at every 100ms active-visible-playing boundary; latest 300 retained.
- Immediate single-step markers without changing the next regular boundary.
- Initial, tenth-marker, and first-topology-in-interval keyframes with early replacement and ≤32 retained.
- Canonical command records and merger verification records at the start-of-step index; expectations verify exactly once and are never applied.
- Exact same-step ordinal ordering around markers.
- Hard ten-entry marker checkpoint LRU.
- Hidden/paused elapsed time produces no markers.

Worker mutation, regenerate, automatic merger, TICK, and STEP paths now feed the timeline. Focused history/engine tests pass 11 tests; real Worker smoke passes 10 tests across Chromium/Firefox. TypeScript and ESLint pass.
