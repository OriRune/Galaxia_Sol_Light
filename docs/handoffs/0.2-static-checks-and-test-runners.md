# Packet handoff

Packet:

- 0.2 - Static checks and test runners

Files created/changed:

- Static-check configuration, dependency rules, Vitest/Playwright projects, coverage/path gates, fixtures, and smoke tests.

Specification requirements:

- SPEC.md section 17.2.

Behavior completed:

- Added every required package script without shell operators, generation math bans, dependency directions, local/hosted browser projects, Istanbul aggregation, and absolute-path scanning.

Tests added:

- Unit, Chromium/Firefox browser, and Chromium/Firefox E2E smoke tests.
- Illegal dependency fixture.
- Coverage pass/fail/pending/required-empty fixtures.
- Windows drive, UNC, Linux/macOS home, and root-relative URL path fixtures.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npm run verify:paths`
- `npm run test`
- `npm run test:browser`
- `npm run test:coverage`
- `npm run coverage:check`
- `npm run e2e:chromium`
- `npm run e2e:firefox`

Results:

- Pass: 11 unit tests, two browser-instance tests, coverage gate, and both E2E projects.
- A later redundant E2E rerun was unavailable because another workspace owned port 5173; the successful packet E2E results predate only fixture-test additions.

Windows/Linux status:

- Windows Chromium and Firefox pass; release-stage Linux verification not yet run.

Golden/constants changed:

- None.

ADR/evidence:

- This handoff.

Remaining packet blockers:

- None.

Next packet:

- 0.3
