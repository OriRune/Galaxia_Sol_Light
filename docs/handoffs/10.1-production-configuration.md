Packet:

- 10.1 Production configuration

Files created/changed:

- `vercel.json`
- `tests/unit/vercel-config.test.ts`
- `scripts/verify-dist-hosting.mjs`
- `e2e/hosted.spec.ts`
- `perf/playwright.hosted-local.config.ts`
- `package.json`

Specification requirements:

- SPEC sections 17 and 19; plan sections 3.4, 17.1, and 17.2.

Behavior completed:

- Added the exact prescribed Vercel Vite build, SPA rewrite, CSP, security headers, and cache policy without COOP or COEP.
- Added local production-artifact checks for root/deep SPA fallback and emitted Worker chunks.
- Added hosted Chromium/Firefox projects covering production startup, WebGL, Worker health, persistence, capture, refresh rediscovery, deep routes, and hosted CSP behavior.
- Kept local CSP assertions skipped under Vite preview because response headers are applied by Vercel; the exact header contract is independently configuration-tested.

Tests added:

- Exact Vercel configuration contract test.
- Local `dist` fallback and Worker artifact verifier.
- Local hosted Chromium/Firefox production smoke and hosted-only CSP fixture blocking test.

Commands run:

- `npm run typecheck`
- `npm run lint`
- Targeted Vercel configuration unit test.
- `npm run build`
- `npm run verify:production-hooks`
- `npm run verify:dist-hosting`
- `npm run e2e:hosted:local`

Results:

- Build, production-hook scan, exact configuration test, root/deep fallback, and Worker artifact checks pass.
- Hosted local production smoke passes in Chromium and Firefox; Vite preview correctly skips the two Vercel-header-only CSP cases.

Windows/Linux status:

- Local Windows Chromium and Firefox production builds pass. Linux hosted evidence remains part of deployment qualification.

Risks or follow-ups:

- Vercel-applied response headers and remote-fixture CSP blocking require the preview deployment in Task 10.2.

Next packet:

- 10.2 Preview deployment
