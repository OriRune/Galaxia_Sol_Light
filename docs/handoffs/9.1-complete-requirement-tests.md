Packet:

- 9.1 Complete requirement tests

Files created/changed:

- `e2e/acceptance/01-startup.spec.ts` through `16-camera.spec.ts` (named scenario files applicable to E2E)
- `tests/unit/05-interaction.acceptance.test.ts`
- `tests/unit/06-merger.acceptance.test.ts`
- `tests/unit/07-black-hole.acceptance.test.ts`
- `tests/unit/12-history.acceptance.test.ts`
- `tests/unit/13-undo.acceptance.test.ts`
- `tests/unit/16-camera.acceptance.test.ts`
- `tests/unit/17-limits.acceptance.test.ts`
- `tests/browser/09-random-reproduction.acceptance.browser.test.ts`
- `src/app/App.tsx`
- `src/simulation/client.ts`
- `src/simulation/engine.ts`
- `src/simulation/history.ts`
- `src/simulation/scheduler.ts`
- `src/simulation/worker.ts`
- `docs/REQUIREMENT_EVIDENCE.md`

Specification requirements:

- All section 19.1 requirement rows and all section 19.2 named scenario groups.
- SPEC sections 4 through 18.

Behavior completed:

- Wired deterministic Random generation, IndexedDB preset/scene save/load/import, screenshots, 30 Hz duration-limited recording, coordinated undo, empty-scene selection/delete/add, rewind UI, and camera controls into the application shell.
- Added bounded one-step Worker task yielding, serialized/coalesced TICK processing, exact relative history boundaries, and pause-aware catch-up draining.
- Rewind reconstruction clones an empty engine before checkpoint restore and preserves exact marker digests.
- Topology mutations use the long operation deadline and heartbeat probes defer while commands are pending.

Tests added:

- Every named acceptance evidence file from section 19.2.
- Browser Random reproduction in Chromium and Firefox.
- Production-size application E2Es for persistence, invalid load, rewind, undo, empty scene, screenshot/300-slot recording, and camera policy.

Commands run:

- `npm run typecheck`
- `npm run lint`
- `npm test -- --project unit --run`
- Focused browser suites in Chromium and Firefox.
- All Chromium acceptance scenarios, followed by focused consolidated reruns for corrected failures.

Results:

- Typecheck and lint pass.
- Unit: 46 files, 265 tests pass.
- Browser Random reproduction passes in Chromium and Firefox.
- Each Chromium acceptance test passes; scenarios 12, 13, and 14 pass serially together after TICK serialization.

Windows/Linux status:

- Windows Chromium evidence green.
- Full Firefox/Edge scenario matrix and Linux remain Task 9.2/9.4.

Risks or follow-ups:

- Reference Low/Balanced performance evidence, Linux verification, soaks, and hosted release remain explicit later packets.

Next packet:

- 9.2 Acceptance scenario suite
