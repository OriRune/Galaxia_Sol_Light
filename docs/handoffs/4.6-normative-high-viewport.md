# Task 4.6 — Normative High viewport performance

Added a self-driving production High harness using the complete Worker, exact five-type/seed fixture at 12,000 stars each, production viewport, trails, automatic framing, and the specified input cycle. It records raw frame intervals and visible input responses and enforces the 30 FPS / 50 ms / 100 ms gates.

Qualification results after 5-second warm-up and 60-second measurement at 1920×1080:

- System Edge 149: 56.52 average FPS, 18.20 ms p95 frame interval, 17.80 ms p95 response.
- Installed system Firefox 152.0.6: 46.41 average FPS, 32.94 ms p95 frame interval, 33.40 ms p95 response.
- Playwright Firefox 151 repeat: 51+ FPS and all gates pass (functional corroboration, not the system-Firefox qualification).

Evidence:

- `docs/evidence/production-high-system-edge.json`
- `docs/evidence/production-high-system-firefox.json`
- `docs/evidence/production-high-firefox.json`

The qualification also found and fixed invalid arm counts in non-arm fixture types, INIT's reconstruction timeout classification, benchmark result publication being delayed behind graceful Worker disposal, and Worker close ordering after `WORKER_DISPOSING`.

Milestone 4 exit gate passes: exact camera/selection tests, all luminance clauses, both reference system browsers, and overlay-free origin-clean artwork capture.
