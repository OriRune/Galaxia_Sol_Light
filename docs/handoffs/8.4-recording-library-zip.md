Packet:

- 8.4 Recording library and ZIP export

Files created/changed:

- `src/capture/recordingExport.ts`
- `src/persistence/recordingRepository.ts`
- `src/app/App.tsx`
- `tests/unit/recording-export.test.ts`
- `tests/unit/recording-repository.test.ts`
- `tests/browser/app-workflows.test.tsx`

Behavior completed:

- Recording summaries remain discoverable after finalization and reload, with state/reason badges and exact captured/missed counts.
- Recording detail exposes metadata, first-frame preview, previous/next captured-frame navigation, unique rename, confirmed resumable deletion, and export planning.
- ZIP parts are metadata-preplanned in slot order with the 63 MiB frame-input budget, repeat the complete manifest, and stream already-compressed frames sequentially.
- Browsers with the File System Access directory picker can write every numbered part after one picker prompt; the fallback exposes one explicit, retryable download per part.
- Object URLs are revoked after downloads and when previews change or unmount.
- Recording creation now allocates collision-safe default names atomically.

Tests and results:

- Recording repository and ZIP unit suites pass (7 tests).
- Browser workflow covers recording discoverability/detail/export controls.
- Complete local production Chromium and Firefox smoke creates a ten-second/300-slot recording, downloads one ZIP part, refreshes, and rediscovers it.

Next packet:

- 8.5 Real and sustained tests
