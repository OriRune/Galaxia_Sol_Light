# Packet handoff

Packet:

- 2.1 - Domain constants, types, and validation

Files created/changed:

- Complete domain types, ranges, defaults, derived values, name handling, strict schemas, validation entry points, version constants, and boundary tests.

Specification requirements:

- SPEC.md sections 2, 4, and 16; IMPLEMENTATION_PLAN.md sections 5, 6.1, and 12.2.

Behavior completed:

- Defines every closed domain union and exact Vec2, generation, galaxy, draft, EngineSetup, SceneSetup, and portable envelope record.
- Centralizes all observable ranges, camera constants, performance budgets, startup defaults, and pure core-radius/scene-total derivations.
- Validates primitive type, finiteness, integer rules, negative-zero canonicalization, closed ranges, arm applicability, galaxy records, duplicate IDs, and scene totals in the required order.
- Trims names, counts Unicode code points without normalizing stored text, and derives a separate NFC lowercase uniqueness key.
- Strictly validates schema-version-1 preset/scene envelopes and rejects all unknown normative fields.
- Returns newly cloned, recursively frozen values without mutating input graphs.

Tests added:

- `tests/unit/domain-validation.test.ts`: 25 tests covering every minimum/default/maximum/just-outside value, invalid primitive and non-finite numbers, arm coupling, IDs, position/velocity, scene counts/totals/duplicates, Unicode boundaries, strict portable schemas, negative zero, immutability, and input preservation.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npx vitest run --project unit tests/unit/domain-validation.test.ts`
- `npm run test`
- `npm run build`

Results:

- Pass. All 25 focused validation tests and the complete unit suite pass; static, architecture, and production-build gates pass.

Windows/Linux status:

- Platform-independent domain suite passes on Windows ARM64; Linux release-stage verification remains scheduled for Milestone 9.

Golden/constants changed:

- None; this packet establishes normative ranges/defaults, not generation goldens.

ADR/evidence:

- `docs/handoffs/2.1-domain-constants-types-validation.md`

Remaining packet blockers:

- None.

Next packet:

- 2.2
