Packet:

- 9.4 Windows/Linux verification

Files created/changed:

- `docs/PLATFORM_RESULTS.md`
- `e2e/acceptance/11-invalid-load.spec.ts`
- `e2e/acceptance/16-camera.spec.ts`

Specification requirements:

- R-PLATFORM-01; SPEC section 17.2; plan sections 2.2, 3.1 through 3.5, and 15.4.

Behavior completed:

- Verified clean native dependency installs and the complete verification pipeline on Windows and Linux.
- Verified the seventeen acceptance/smoke tests in current system Edge on Windows.
- Smoked the production artifact in current stable system Firefox on both OSes, covering startup, renderer/canvas readiness, scene save/load, and capture persistence.
- Stabilized the low-fixture invalid-import oracle and DOM wheel injection across Chromium, Firefox, and Edge.

Tests added:

- No new requirement test; two existing cross-browser E2E oracles were corrected and rerun in all three Windows browser projects.

Commands run:

- Windows: `npm ci`
- Windows: `npx playwright install chromium firefox`
- Windows: `npm run verify`
- Windows: `npm run e2e:edge`
- Linux: `npm ci`
- Linux: `npx playwright install --with-deps chromium firefox`
- Linux: `npm run verify`
- System Firefox production-artifact smoke on Windows and Linux.

Results:

- Windows verify: 290 unit, 45 browser, Chromium 17/17, Firefox 17/17; all coverage and build gates pass.
- Windows Edge: 17/17.
- Linux verify: 290 unit, 45 browser, Chromium 17/17, Firefox 17/17; all coverage and build gates pass.
- System Firefox 152.0.6 smoke: pass on Windows and Linux.

Windows/Linux status:

- Windows green.
- Linux green.

Risks or follow-ups:

- Linux evidence was recorded on Ubuntu 26.04 LTS under WSL2 ARM64; no functional non-conformance was observed.

Next packet:

- 9.5 Soak, accessibility, and report
