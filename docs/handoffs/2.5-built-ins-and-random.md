# Packet handoff

Packet:

- 2.5 - Built-ins and Random

Files created/changed:

- Immutable built-in preset assets, deterministic Random scenario construction, and exact tests.

Behavior completed:

- Defines First Light and all five prescribed built-in configurations exactly.
- Derives independent root/child/ID streams from category, seed, and performance level.
- Implements exact single, collision, and 3-through-5 cluster allocations at all three performance budgets.
- Generates explicit child configurations, deterministic collision-safe IDs, prescribed placement/orbits, and Float32-rounded scalars.
- Removes mass-weighted velocity means for collision and both barycenter and momentum for clusters.

Tests added:

- `tests/unit/presets-random.test.ts`: exact built-ins, immutability, all nine category/level combinations, budget/remainder allocation, cluster count, IDs, barycenter/momentum, repeatability, and pinned setup SHA-256.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npm test`
- `npm run build`

Results:

- Pass: 10 unit files and 82 tests; architecture and production build pass.
- Pinned Random setup digest: `7c57c5c5fba0f3197c9bc543990fd263962228dea0825b4f899c3f565ba6542d`.

Windows/Linux status:

- Windows complete; Linux release-stage verification remains scheduled for Milestone 9.

Golden/constants changed:

- Added one exact Random setup digest.

Remaining packet blockers:

- None.

Next packet:

- 2.6
