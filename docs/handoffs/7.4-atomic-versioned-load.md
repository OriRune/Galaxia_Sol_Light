# Task 7.4 — Atomic versioned load

- Added strict structural validation with explicit generation-version confirmation.
- Added snapshot-first load, paused Builder success state, selection clear, framing enable, and one coordinated undo entry.
- Post-ACK/UI failures restore both UI and exact Worker snapshot; pre-validation failures request no snapshot.
