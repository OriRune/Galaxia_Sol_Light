# Task 8.2 — Recording preflight and scheduler

- Added exact five-sample p95 quota math, WebP/PNG path contract, 300/3600 limits, and two-target saturation.
- Added wall-clock floor scheduler, newest-only attempts, hidden-slot loss, and exact nominal timestamps.

Product integration audit: Record runs a real five-frame sequential encoder preflight, requests persistent storage, estimates quota, refuses effective capacity below 300, and uses exactly two busy offscreen targets. The 30Hz wall-clock scheduler records hidden/saturated slots as missed, never queues backlog, displays elapsed/captured/missed/effective-limit status, and stops at duration, quota, or three consecutive encoder failures. WebP failure falls back to PNG; the deterministic 300-slot override is compiled only into test builds.
