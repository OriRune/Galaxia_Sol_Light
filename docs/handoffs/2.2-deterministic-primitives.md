# Packet handoff

Packet:

- 2.2 - Deterministic primitives

Files created/changed:

- 32-bit mixing/hashing, Mulberry32, deterministic math, sine generator/artifact/loader/digest, canonical generation encoding/digest, bootstrap integration, complete lint policy, Node test setup, and vector tests.

Specification requirements:

- SPEC.md sections 4.1 and 17.2; IMPLEMENTATION_PLAN.md sections 6 and 15.5.

Behavior completed:

- Implements unsigned `mix32`, domain-separated `hashWords`, little-endian Float64 words, enum/config word encoding, and Mulberry32.
- Commits a 65,536-entry little-endian Float32 sine table generated once under Node 24 with SHA-256 verification before installation.
- Exposes interpolated `sinTurn`/`cosTurn` with stable not-ready/invalid errors and installs the artifact before browser UI or Worker readiness.
- Implements exactly-12-draw `normalLike` and host-independent little-endian canonical generation bytes/SHA-256.
- Prohibits Math.random, all 22 listed nondeterministic Math functions, and exponentiation throughout `src/generation`.

Tests added:

- `tests/unit/deterministic-primitives.test.ts`: required PRNG vectors, three fixed/independent hash vectors, Float64 word reference, sine byte digest/quadrants/wrap, exact draw count, canonical DataView encoding/digest with offset source buffers, all 24 lint violations, and isolated not-ready error.
- `tests/unit/setup.ts`: reads and installs the committed artifact for Node generation tests without production Node imports.

Commands run:

- `node scripts/generate-sine-table.mjs`
- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npx vitest run --project unit tests/unit/deterministic-primitives.test.ts`
- `npx vitest run --project browser tests/browser/worker-smoke.test.ts tests/browser/app-health.test.tsx`
- `npm run test`
- `npm run build`

Results:

- Pass. All required vectors, independent references, artifact checks, canonical encoding, and complete prohibited-operation fixture pass.
- Artifact is exactly 262,144 bytes with SHA-256 `cdfaec6870788e193dff3f1ddfa3ca7d3f1a897042d78365a1fe3a433be3627d`; regeneration printed the identical digest.
- Production build emits the committed binary as a 262.14-kB asset; UI and Worker browser smoke tests pass after verified installation.

Windows/Linux status:

- Node and Windows Chromium/Firefox verification complete; Linux release-stage verification remains scheduled for Milestone 9.

Golden/constants changed:

- Added the normative committed sine-table digest and binary artifact; no generation output golden exists yet.

ADR/evidence:

- `src/generation/generated/sine-f32.bin`
- `src/generation/sineTableDigest.ts`

Remaining packet blockers:

- None.

Next packet:

- 2.3
