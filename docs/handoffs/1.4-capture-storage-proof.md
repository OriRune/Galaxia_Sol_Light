# Packet handoff

Packet:

- 1.4 - Capture/storage proof

Files created/changed:

- Full-HD capture/storage prototype, real-browser proof, measurement evidence, and ADR 0003.

Specification requirements:

- SPEC.md sections 14.2 and 17.2; IMPLEMENTATION_PLAN.md sections 12.1, 13.1–13.8, and 15.7.

Behavior completed:

- Creates 300 artwork-like 1920 by 1080 frames through exactly two reusable offscreen targets.
- Detects non-empty WebP support and measures nearest-rank p95 Blob size, `toBlob` latency, and IndexedDB write latency.
- Immediately persists each successful frame and proves a saturated third operation becomes a missed slot.
- Plans ZIP parts from metadata, streams already-compressed frames sequentially, and creates a real near-63-MiB input part below the 64-MiB output cap.
- Projects the 3,600-slot part count from measured p95 size and the exact 63-MiB formula.
- Proves one explicit fallback action creates exactly one part and deletes the unique spike database before returning.

Tests added:

- `tests/browser/capture-storage.test.ts`: Chromium and Firefox full 300-frame encode/write/ZIP/delete proof with measured and bounded oracles.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npx vitest run --project browser tests/browser/capture-storage.test.ts`
- `npm run build`

Results:

- Pass in Chromium: WebP, 75,330-byte p95 frame, 185 ms encode p95, 2.3 ms write p95, five projected full-limit parts, 82,970,562-byte peak owned memory.
- Pass in Firefox: WebP, 76,018-byte p95 frame, 217 ms encode p95, 9 ms write p95, five projected full-limit parts, 82,996,680-byte peak owned memory.
- Pass: maximum two in flight, one forced missed slot, near-63-MiB streamed ZIP parts below 64 MiB, exactly one explicit fallback build, and spike database deleted.

Windows/Linux status:

- Windows Chromium and Firefox browser proof complete; Linux release-stage verification remains scheduled for Milestone 9.

Golden/constants changed:

- None.

ADR/evidence:

- `docs/adr/0003-recording-storage.md`
- `docs/evidence/capture-storage-proof.json`

Remaining packet blockers:

- None.

Next packet:

- 1.5
