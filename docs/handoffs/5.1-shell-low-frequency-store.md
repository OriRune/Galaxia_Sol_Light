# Task 5.1 — Shell and low-frequency store

Implemented the exact desktop CSS grid with 52px top bar, clamped side panels, minimum viewport, collapsed timeline, and the 1024px mutually exclusive 260px drawer layout. The top playback controls remain visible and drawer controls are explicit buttons.

Added the Zustand low-frequency application store containing only the documented UI state. Worker frame callbacks apply directly to `PixiViewport`; star/core frame arrays never enter React or Zustand. Status commits are limited to 10 Hz, and the accessible canvas summary derives from that low-frequency status.

Added the non-React `UndoStore` coordinator with snapshot IDs/UI snapshots, depth reporting, oldest release after twenty entries, and no Worker buffers in Zustand.

Verification:

- TypeScript and ESLint pass.
- Store/undo focused unit tests pass.
- App health and accessible shell regression passes in Chromium and Firefox (4 tests).

Product integration audit (2026-07-16): the desktop shell uses the specified 52px/side-panel/viewport/bottom-strip grid, with a 56px active history/recording strip. The right panel now has the required Selection, Presets, Scenes, Captures, and Recordings tabs; the 1024px drawer path keeps library content reachable. The always-visible status includes playing state, selected speed, gravity, performance level, counts, FPS, selection context, and Worker/renderer health.
