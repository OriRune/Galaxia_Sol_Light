# Packet handoff

Packet:

- 1.2 - Transfer pool proof

Files created/changed:

- `perf/transfer-pool.ts`
- `perf/transfer-pool.worker.ts`
- `tests/browser/transfer-pool.test.ts`

Specification requirements:

- SPEC.md sections 8.1 and 17.2; IMPLEMENTATION_PLAN.md sections 9.4 and 15.7.

Behavior completed:

- Allocates exactly three reusable interleaved position buffers and never transfers authoritative arrays.
- Publishes at most one outstanding FRAME in response to a TICK while every TICK still advances simulation.
- Drops publication opportunities while the main thread retains a lease, preventing FRAME backlog.
- Returns transferred buffers through a main-thread `finally` block even when frame application throws.
- Rebuilds exactly once after three opportunities with an unreturned lease, invalidates prior-generation IDs, and discards late old buffers.
- Exposes proof counters for publications, drops, rebuilds, late returns, and current/maximum live leases.

Tests added:

- Real module Worker completes 10,000 publish/return cycles in Chromium and Firefox with exactly three live leases.
- Forced slow main thread advances three simulation ticks while dropping two publications.
- Injected `applyFrame` exception still posts RETURN.
- Three missed opportunities emit exactly one FRAME_TRANSPORT rebuild; a late old lease is ignored and the replacement generation remains exactly three buffers.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npx vitest run --project browser tests/browser/transfer-pool.test.ts`
- `npm run build`

Results:

- Pass in both configured real browsers: 20,000 aggregate publish/return cycles, zero rebuilds during the soak, no fourth live lease, and all fault-injection oracles satisfied.

Windows/Linux status:

- Windows Chromium and Firefox browser proof complete; Linux release-stage verification remains scheduled for Milestone 9.

Golden/constants changed:

- None.

ADR/evidence:

- `docs/handoffs/1.2-transfer-pool-proof.md`

Remaining packet blockers:

- None.

Next packet:

- 1.3
