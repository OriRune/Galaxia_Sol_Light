# Packet handoff

Packet:

- 3.7 - Full calibration, final goldens, and Milestone 3 gate

Files created/changed:

- Complete ordered fallback-grid definition/test, accepted calibration ADR, full evidence record, and final digest verification.

Behavior completed:

- Reruns the required starting tuple through core references and full production star fixtures.
- Defines all 324 fallback tuples in the exact mandated nested order; none are evaluated because the starting tuple passes every gate.
- Locks the calibrated constants and accepts ADR 0004.
- Promotes the ten generation digests to final after fresh Windows Chromium/Firefox byte equality.

Commands run:

- `npm test`
- `npm run test:browser -- --run tests/browser/generation-determinism.test.ts`
- `node scripts/compare-generation-digests.mjs docs/evidence/generation-digests-windows-chromium.json docs/evidence/generation-digests-windows-firefox.json`
- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npm run build`

Results:

- Pass: 16 unit files and 166 tests; generation digest pack passes in Windows Chromium and Firefox; architecture and production build pass.
- Milestone 3 gate passes: full physics margins pass together, Worker owns simulation, recovery/error paths pass, constants are locked, and final goldens match cross-browser.

Windows/Linux status:

- Windows Chromium and Firefox complete. Linux final conformance remains scheduled for Milestone 9.

Golden/constants changed:

- No numeric change was required. The starting tuple is final, and `tests/fixtures/generation-digests.json` is now the locked final pack.

Remaining packet blockers:

- None.

Next packet:

- 4.1
