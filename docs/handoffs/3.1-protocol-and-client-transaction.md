# Packet handoff

Packet:

- 3.1 - Complete protocol and client transaction

Files created/changed:

- Complete closed request/signal/event types, strict boundary schemas, Worker protocol adapter, revision/snapshot rules, client request coordinator, heartbeat, and transaction tests.

Behavior completed:

- Defines and validates all 27 acknowledged commands and exactly three fire-and-forget signals.
- Enforces protocol version, duplicate request IDs, null/revision rules, stale revisions, one active snapshot lock, cancellation, and stable ACK/REJECT responses.
- Tracks a monotonic acknowledged model revision and ignores late responses after request timeout/removal or Worker replacement.
- Runs visible-page heartbeat pings and declares failure after three unanswered heartbeats.
- Stages command-correlated topology/delta until READY/ACK, discards staged state on failure, and commits matching automatic topology/delta pairs atomically.
- Rejects missing required command transaction events and mismatched automatic pairs as `PROTOCOL_SEQUENCE`.

Tests added/expanded:

- `tests/unit/protocol-schema.test.ts`: valid and invalid payload coverage for every command and all three signals.
- `tests/browser/protocol-transaction.test.ts`: revisions, stale rejection, snapshot lock/release, and duplicate request IDs in Chromium/Firefox.
- `tests/browser/simulation-client-errors.test.ts`: stale/late event defenses, timeouts, Worker failures, command staging, and automatic-pair atomicity.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npm test`
- `npm run test:browser`
- `npm run build`

Results:

- Pass: 137 unit tests, 52 browser tests across Chromium/Firefox, production build, and architecture gate.
- Focused post-refactor protocol browser pack: 26 tests pass in both engines.

Windows/Linux status:

- Windows Chromium and Firefox complete; Linux conformance remains scheduled for Milestone 9.

Golden/constants changed:

- None.

Remaining packet blockers:

- None. Engine-backed topology content and snapshot bytes are introduced by Task 3.2 through this protocol contract.

Next packet:

- 3.2
