# Packet handoff

Packet:

- 3.3 - Core physics and scheduler

Files created/changed:

- Fixed simulation constants, production core acceleration/friction, Engine two-bank step, visible-time scheduler, Worker signal integration, and tests.

Behavior completed:

- Imports the shared Plummer contract and accumulates unordered core pairs in scene order.
- Applies calibrated snapshot/accumulate overlap friction twice per step, conserves momentum, and propagates each owner's summed impulse to its stars once.
- Executes fixed kick-drift-kick core steps through reusable current/candidate banks with full finite validation and last-valid preservation.
- Splits elapsed visible time at exact 100-ms boundaries, retains fractional steps, maps all five speeds exactly, yields after eight steps, and detects sustained backlog overload.
- Hidden, paused, and mutation-locked intervals reset origins and never catch up; single-step advances once with zero active wall time.
- Client charges PLAY/PAUSE/speed transitions to the old state by sending TICK first.

Tests added:

- `tests/unit/core-physics-scheduler.test.ts`: calibrated default attraction, invalid-candidate preservation, 3/12-core momentum, all speed mappings, marker splits/yields, visibility/lock behavior, and single-step.

Commands run:

- `npm run format:check`
- `npm run typecheck`
- `npm run lint`
- `npm run architecture:check`
- `npm test`
- Focused Chromium/Firefox Worker/app tests
- `npm run build`

Results:

- Pass: 13 unit files and 150 tests; 18 focused browser tests; architecture and production build pass.
- Production default-attraction separation matches `94.91200877` within 1e-6.

Windows/Linux status:

- Windows Chromium and Firefox complete; Linux conformance remains scheduled for Milestone 9.

Golden/constants changed:

- Added the locked calibrated simulation-only constants; generation constants remain unchanged.

Remaining packet blockers:

- None.

Next packet:

- 3.4
